package com.jinnova.docaid;

public class DynamicFieldSet extends FieldSet {
	
	//private final HashMap<FieldFunction, FieldFunction> allFields = new HashMap<>();
	//private ArrayList<FieldFunction> allFields = new ArrayList<>();

	@Override
	public void copy(FieldFunction fieldFunction, boolean forced) {
		/*if (!allFields.containsKey(fieldFunction)) {
			allFields.put(fieldFunction, fieldFunction);
			return;
		}*/
		//not supported
	}

}
